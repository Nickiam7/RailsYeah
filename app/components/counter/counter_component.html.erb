<div class="card" data-controller="counter">
  <div class="card-body d-flex align-items-center gap-3">
    <button type="button" class="btn btn-primary" data-action="counter#increment">
      <%= label %>
    </button>
    <span class="text-body-secondary" data-counter-target="output">Stimulus not connected</span>
  </div>
</div>
