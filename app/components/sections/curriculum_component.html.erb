<section id="curriculum" class="section bg-body-tertiary">
  <div class="container">
    <div class="d-flex justify-content-between align-items-end flex-wrap gap-2">
      <%= render Heading::SectionHeadingComponent.new(eyebrow: "Curriculum", title: "Course content") %>
      <span class="fw-semibold text-body-secondary"><%= totals %></span>
    </div>

    <div class="curriculum mt-2">
      <% sections.each do |section| %>
        <div class="curriculum-section d-flex justify-content-between align-items-center gap-3">
          <span class="fw-semibold"><%= section.title %></span>
          <span class="curriculum-meta text-body-secondary small text-nowrap"><%= meta_for(section) %></span>
        </div>
      <% end %>
    </div>
  </div>
</section>
