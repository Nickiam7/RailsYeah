<div class="<%= wrapper_classes %>">
  <%= content_tag heading_tag, title, class: "fw-bold" %>
  <% if subtitle.present? %>
    <p class="lead text-body-secondary mb-0"><%= subtitle %></p>
  <% end %>
</div>
